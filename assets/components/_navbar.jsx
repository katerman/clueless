var React = require('react');

var Navbar = function(props){

	return (
		<nav className="navbar navbar-default text-center">
			<a className="navbar-brand" href="#">Clueless</a>
		</nav>
	);


};

module.exports = Navbar;
